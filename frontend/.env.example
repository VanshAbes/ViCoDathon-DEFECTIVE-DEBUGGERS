# Not consumed yet — API integration is not implemented in this pass.
# When wiring `useInterviewSession` to the real backend, point this at
# wherever the FastAPI/Express/etc. service defined in technical-spec.md runs.
VITE_API_BASE_URL=http://localhost:8000
